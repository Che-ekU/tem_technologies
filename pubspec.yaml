name: tem_technologies
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  file_picker: ^6.2.0
  firebase_core: ^2.26.0
  firebase_storage: ^11.6.8
  flutter:
    sdk: flutter
  video_player: ^2.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
